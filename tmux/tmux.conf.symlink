unbind C-b
set -g prefix C-a

# https://wiki.archlinux.org/index.php/Tmux#Setting_the_correct_term
set -g default-terminal "tmux-256color" # use tmux-256color terminfo inside tmux sessions
set -ga terminal-overrides ",xterm-256color*:Tc" # add/extend TrueColor support to xterm-256color terminfo
